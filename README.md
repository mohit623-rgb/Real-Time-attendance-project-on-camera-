📊 Smart Attendance System (Face Recognition + CSV + Dashboard)
📌 Project Overview

This project is a real-time attendance system built using:

Frontend: HTML, CSS
Backend: Django
Camera Processing: OpenCV (Python)

It captures faces through a webcam, recognizes users, and automatically stores attendance in a CSV file with date and time.
